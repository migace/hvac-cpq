{
  "name": "@hvac-cpq/backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "ruff check ."
  }
}
